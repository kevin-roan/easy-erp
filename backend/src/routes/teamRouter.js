const express = require("express");

const router = express.Router();

const addNewTeam = (teamData) => {};
const deleteTeamById = (teamId) => {};
const updateTeamById = (teamId, teamData) => {};

module.exports = { addNewTeam, deleteTeamById, updateTeamById };
